apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: training-webhook
  namespace: argo-events
spec:
  eventBusName: default

  service:
    ports:
      - port: 12000
        targetPort: 12000

  webhook:
    training:
      port: "12000"
      endpoint: /train
      method: POST
